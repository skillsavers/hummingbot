# VPS Connection Settings
VPS_HOST=46.101.135.160
VPS_USER=root

# GitHub Container Registry Authentication (for private images)
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required scopes: read:packages
GITHUB_USERNAME=your_github_username
GITHUB_TOKEN=ghp_your_github_token_here

# Hummingbot Configuration (optional)
# Uncomment and configure if you want to set these via environment
# CONFIG_PASSWORD=your_password_here
# CONFIG_FILE_NAME=your_config.py
# SCRIPT_CONFIG=your_script_config.yml

# Watchtower Settings
# How often to check for updates (in seconds, default 3600 = 1 hour)
WATCHTOWER_POLL_INTERVAL=3600

# Nginx Settings
# Add custom settings here if needed

# SSL/HTTPS Settings (configure after obtaining certificates)
# DOMAIN_NAME=yourdomain.com
# SSL_CERT_PATH=/etc/nginx/ssl/fullchain.pem
# SSL_KEY_PATH=/etc/nginx/ssl/privkey.pem
